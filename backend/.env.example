# ========================================
# ARCHIVO .env.example
# ========================================
# Este es un TEMPLATE del archivo .env
# Copia este archivo a .env y completa con tus valores reales
#
# IMPORTANTE: NUNCA subir el archivo .env real a Git
# ========================================

# ========================================
# CONFIGURACIÓN DEL SERVIDOR
# ========================================
# Puerto en el que correrá el servidor Express
PORT=4000

# Ambiente de ejecución (development, production, test)
NODE_ENV=development

# ========================================
# BASE DE DATOS MONGODB
# ========================================
# URL de conexión a MongoDB (local o Atlas)
# 
# Formato para MongoDB Atlas:
# mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/<nombre_bd>
#
# Formato para MongoDB local:
# mongodb://localhost:27017/<nombre_bd>
#
# IMPORTANTE: Reemplazar con tus credenciales reales
DB_CDN=mongodb+srv://usuario:password@cluster.mongodb.net/nombre_base_datos

# ========================================
# JSON WEB TOKEN (JWT)
# ========================================
# Clave secreta para firmar los tokens JWT
# Debe ser una cadena aleatoria y compleja
# Puedes generarla con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET_JWT_SEED=tu_clave_secreta_super_segura_aqui_12345

# Tiempo de expiración del token (opcional)
# Ejemplos: 2h (2 horas), 7d (7 días), 30m (30 minutos)
JWT_EXPIRES_IN=2h

# ========================================
# CORS (opcional)
# ========================================
# URL del frontend para configurar CORS
# En desarrollo: http://localhost:5173 (Vite)
# En producción: tu dominio real
FRONTEND_URL=http://localhost:5173

# ========================================
# CONFIGURACIÓN DE EMAIL (futuro)
# ========================================
# Si planeas enviar emails (recuperación de contraseña, etc.)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=tu_email@gmail.com
# EMAIL_PASSWORD=tu_password_de_aplicacion

# ========================================
# OTRAS CONFIGURACIONES
# ========================================
# Límite de tamaño para uploads (opcional)
# MAX_FILE_SIZE=5mb

# Nivel de logs (opcional)
# LOG_LEVEL=debug